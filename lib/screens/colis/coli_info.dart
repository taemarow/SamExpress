
import 'package:flutter/material.dart';

class ColiInfo extends StatelessWidget {
  const ColiInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [

        ],
      ),
    );
  }
}